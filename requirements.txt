# Runtime dependencies.
#
# The analysis pipeline itself is pure standard library — the scoring,
# conservation, charge, physics-tier and transformation modules import nothing
# outside stdlib, and the CLI runs with no dependencies at all. Everything below
# is for the web interface only.
#
# Versions are lower bounds rather than exact pins: FastAPI dictates which
# Pydantic it needs, and pinning Pydantic independently produces a conflict.

fastapi>=0.110
uvicorn[standard]>=0.27

# Needed once live retrieval is wired up (NCBI Entrez homologs, UniProt, PubMed,
# Human Protein Atlas). Not imported yet, so not installed yet:
#   biopython>=1.83
#   requests>=2.31

# ML layer (Addendum 3). Optional, and left commented deliberately: the analysis
# pipeline and the web interface run without it, `peptide_suite` never imports
# `ml`, and the two ML endpoints report the layer as unavailable rather than
# failing. Install it to run ml/ and its tests:
#   pip install torch          (2.14 here, CPU; ml/tests need it)
